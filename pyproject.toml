[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mateex"
version = "1.0.0"
description = "Libreria matematica hecha por Nico"
authors = [
  {name = "Nico"}
]